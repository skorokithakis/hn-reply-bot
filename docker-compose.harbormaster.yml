services:
  server:
    command: bash -c "cd /state/; /code/bot.py"
    ports:
      - "27838:8000"
    volumes:
      - {{ HM_DATA_DIR }}:/state
  worker:
    command: bash -c "cd /state/; /code/bot.py worker"
    volumes:
      - {{ HM_DATA_DIR }}:/state
